# Planner Capability Audit — v80 failures only

_Generated by `scripts/audit_planner_capability.py`._

## Headline counts

- A (silent acceptance, ≤2 iter + judge=finish): **9**
- B (persistent struggle, multi-iter, judge guidance ignored): **7**
- C (code-error exhaustion): **1**
- D (other/edge): **0**

Each category needs a different intervention:
- A → stronger pre-finish gate (Skeptic-veto style; HarnessGate coverage)
- B → Planner technical capability (helper signposting; concrete recipes)
- C → Debugger upgrades or Planner robustness

---

## Category A — Silent Acceptance — 9 tasks

### task_169 (medium, 1 iter)
**Q**: What was the average monthly consumption of customers in SME for the year 2013?

**Last Judge reasoning** (truncated): The answer is present as a scalar value (8.202722e+07). The SQL logic correctly implements the domain formula from knowl

**Final code (first 240 chars)**:
```
SELECT SUM(ym.Consumption) / 12.0 as average_monthly_consumption
FROM yearmonth ym
JOIN customers c ON ym.CustomerID = c.CustomerID
WHERE c.Segment = 'SME' 
  AND ym.Date >= 201301 
  AND ym.Date <= 201312
```
**Helper opportunities** (by keyword match): `(none detected)`
**Helpers actually used in final code**: `(none)`


### task_180 (medium, 2 iter)
**Q**: For all the people who paid more than 29.00 per unit of product id No.5. Give their consumption status in the August of 2012.

**Last Judge reasoning** (truncated): The answer is present with 153 consumption values. The logic correctly: (1) filters transactions for ProductID=5 and Pri

**Final code (first 240 chars)**:
```
SELECT DISTINCT y.Consumption
FROM transactions_1k t
JOIN yearmonth y ON t.CustomerID = y.CustomerID
WHERE t.ProductID = 5 
  AND t.Price > 29.00
  AND strftime('%Y%m', t.Date) = '201208'
  AND y.Date = 201208
ORDER BY y.Consumption
```
**Helper opportunities** (by keyword match): `(none detected)`
**Helpers actually used in final code**: `(none)`


### task_200 (medium, 1 iter)
**Q**: Calculate the total atoms with triple-bond molecules containing the element phosphorus or bromine.

**Last Judge reasoning** (truncated): The answer is present (4 total atoms), the logic is correct (finds molecules with triple bonds AND containing p or br, t

**Final code (first 240 chars)**:
```
SELECT COUNT(*) as total_atoms
FROM atom
WHERE molecule_id IN (
    SELECT DISTINCT b.molecule_id
    FROM bond b
    WHERE b.bond_type = '#'
    AND EXISTS (
        SELECT 1 FROM atom a 
        WHERE a.molecule_id = b.molecule_id 
      
```
**Helper opportunities** (by keyword match): `(none detected)`
**Helpers actually used in final code**: `(none)`


### task_243 (medium, 1 iter)
**Q**: For the user No.24, how many times is the number of his/her posts compared to his/her votes?

**Last Judge reasoning** (truncated): The answer is present (0.103448), the logic correctly implements the Post-to-Vote Ratio formula from the Domain Rules (D

**Final code (first 240 chars)**:
```
WITH user_posts AS (
    SELECT COUNT(*) as post_count 
    FROM posts 
    WHERE OwnerUserId = 24
),
user_votes AS (
    SELECT COUNT(*) as vote_count 
    FROM votes v 
    INNER JOIN posts p ON v.PostId = p.Id 
    WHERE p.OwnerUserId = 
```
**Helper opportunities** (by keyword match): `(none detected)`
**Helpers actually used in final code**: `(none)`


### task_25 (easy, 2 iter)
**Q**: Which event has the lowest cost?

**Last Judge reasoning** (truncated): The answer correctly identifies 3 events tied at the lowest cost. The HarnessGate warnings about [agg_type] and [sql_col

**Final code (first 240 chars)**:
```
WITH event_costs AS (
    SELECT 
        e.event_id,
        e.event_name,
        SUM(ex.cost) as total_cost
    FROM expense ex
    JOIN budget b ON ex.link_to_budget = b.budget_id
    JOIN event e ON b.link_to_event = e.event_id
    GRO
```
**Helper opportunities** (by keyword match): `(none detected)`
**Helpers actually used in final code**: `(none)`


### task_344 (hard, 1 iter)
**Q**: Among the male patients who have a normal level of white blood cells, how many of them have an abnormal fibrinogen level?

**Last Judge reasoning** (truncated): The query correctly computes the count of male patients with normal WBC levels (4.0-11.0) who have abnormal fibrinogen l

**Final code (first 240 chars)**:
```
SELECT COUNT(*) as cnt
FROM patient_sex ps
JOIN Laboratory l ON ps.ID = l.ID
WHERE ps.SEX = 'M'
  AND l.WBC >= 4.0 AND l.WBC <= 11.0
  AND (l.FG < 2.0 OR l.FG > 4.0)
```
**Helper opportunities** (by keyword match): `(none detected)`
**Helpers actually used in final code**: `(none)`


### task_379 (hard, 2 iter)
**Q**: Tally the toxicology element of the 4th atom of each molecule that was carcinogenic.

**Last Judge reasoning** (truncated): The answer is complete and correctly formatted. The code properly: (1) extracted carcinogenic molecule IDs from the docu

**Final code (first 240 chars)**:
```
WITH carcinogenic_molecules AS (
    SELECT 'TR391' as molecule_id UNION ALL SELECT 'TR483' UNION ALL SELECT 'TR342' UNION ALL SELECT 'TR316' UNION ALL SELECT 'TR039' UNION ALL SELECT 'TR414' UNION ALL SELECT 'TR309' UNION ALL SELECT 'TR164
```
**Helper opportunities** (by keyword match): `(none detected)`
**Helpers actually used in final code**: `(none)`


### task_86 (easy, 1 iter)
**Q**: Which race was Alex Yoong in when he was in track number less than 20?

**Last Judge reasoning** (truncated): No red flags found. The query correctly joins drivers, driverStandings, and races tables using proper keys (driverId, ra

**Final code (first 240 chars)**:
```
SELECT DISTINCT r.name AS race_name
FROM drivers d
JOIN driverStandings ds ON d.driverId = ds.driverId
JOIN races r ON ds.raceId = r.raceId
WHERE LOWER(d.forename) = 'alex' 
  AND LOWER(d.surname) = 'yoong'
  AND r.round < 20
ORDER BY r.rou
```
**Helper opportunities** (by keyword match): `(none detected)`
**Helpers actually used in final code**: `(none)`


### task_89 (easy, 2 iter)
**Q**: What's the finish time for the driver who ranked second in 2008's Chinese Grand Prix?

**Last Judge reasoning** (truncated): The query successfully returns a single scalar value (14.925) for the finish time of the driver who ranked second in the

**Final code (first 240 chars)**:
```
SELECT res.time
FROM races r
JOIN results res ON r.raceId = res.raceId
WHERE r.year = 2008 
  AND r.name = 'Chinese Grand Prix'
  AND res.positionOrder = 2
```
**Helper opportunities** (by keyword match): `(none detected)`
**Helpers actually used in final code**: `(none)`


---

## Category B — Persistent Struggle (KEY TO DIG) — 7 tasks

### task_163 (medium, 5 iter)
**Q**: Identify the type of expenses and their total value approved for 'October Meeting' event.

**Last Judge reasoning** (truncated): The answer is present and complete. The code successfully joined expense.csv with budget.json and event.db, filtered for

**Final code (first 240 chars)**:
```
import pandas as pd
from data_helpers import safe_read_csv, save_result
import duckdb
import json

# Read all data sources
expense_df = safe_read_csv("context/csv/expense.csv")

# Handle JSON structure properly
with open("context/json/budge
```
**Helper opportunities** (by keyword match): `safe_read_json_df`
**Helpers actually used in final code**: `(none)`


### task_199 (medium, 3 iter)
**Q**: List the names and funding types of schools from Riverside-related school districts where the average SAT math score across schools exceeds 400.

**Last Judge reasoning** (truncated): Answer is present with 61 schools found. The logic correctly: (1) joins FRPM and SAT scores data on CDSCode, (2) filters

**Final code (first 240 chars)**:
```
# === CODE VALIDATOR WARNINGS ===
# WARNING: Column 'CDSCode_str' not found in manifest — close matches: ['CDSCode', 'cds']
# WARNING: Column 'cds_str' not found in manifest — close matches: ['CDSCode', 'cds']
# Verify column names before r
```
**Helper opportunities** (by keyword match): `(none detected)`
**Helpers actually used in final code**: `(none)`


### task_257 (medium, 8 iter)
**Q**: Identify the total views on the post 'Computer Game Datasets'. Name the user who posted it last time.

**Last Judge reasoning** (truncated): The current answer only provides the total_views (1708) but is missing the second required piece of information: the nam

**Final code (first 240 chars)**:
```
SELECT CAST(ViewCount AS INTEGER) as total_views
FROM posts
WHERE LOWER(Title) LIKE '%computer game datasets%'
LIMIT 1
```
**Helper opportunities** (by keyword match): `safe_read_json_df`
**Helpers actually used in final code**: `(none)`


### task_352 (hard, 8 iter)
**Q**: How many times was the budget in Advertisement for "Yearly Kickoff" meeting more than "October Meeting"?

**Last Judge reasoning** (truncated): Both budget values show $0, which is highly suspicious given that Steps 3-4 explicitly found '2 target events'. The guid

**Final code (first 240 chars)**:
```
import json
from data_helpers import save_result

def safe_read_json(filepath):
    """Safely read JSON file with error handling"""
    try:
        with open(filepath, 'r', encoding='utf-8') as f:
            data = json.load(f)
        re
```
**Helper opportunities** (by keyword match): `safe_read_json_df`
**Helpers actually used in final code**: `(none)`


### task_396 (hard, 7 iter)
**Q**: In superheroes with height between 150 to 180, what is the percentage of heroes published by Marvel Comics?

**Last Judge reasoning** (truncated): The answer is present as a scalar percentage value (0.0%). The query logic is correct: it joins superhero and publishers

**Final code (first 240 chars)**:
```
import duckdb
from data_helpers import save_result

# Connect to the database
conn = duckdb.connect('/Users/bingxuan/Desktop/repo/data_analytics_agent/public/input/task_396/context/database.db')

# Query to calculate percentage of Marvel Co
```
**Helper opportunities** (by keyword match): `(none detected)`
**Helpers actually used in final code**: `(none)`


### task_408 (hard, 8 iter)
**Q**: How much faster in percentage is the champion than the driver who finished the race last in the 2008 Australian Grand Prix?

**Last Judge reasoning** (truncated): The latest step failed with 'Error: no such table: races' - the database structure doesn't match expectations. No actual

**Final code (first 240 chars)**:
```
import sqlite3
import os

# Database paths
context_db_path = '/Users/bingxuan/Desktop/repo/data_analytics_agent/public/input/task_408/context/db'
races_db_path = os.path.join(context_db_path, 'races.db')
results_db_path = os.path.join(conte
```
**Helper opportunities** (by keyword match): `(none detected)`
**Helpers actually used in final code**: `(none)`


### task_418 (extreme, 4 iter)
**Q**: Among the patients whose creatinine level is abnormal, how many of them aren't 70 yet?

**Last Judge reasoning** (truncated): The answer is present and properly formatted as a scalar count. The code successfully read the pre-computed JSON file wh

**Final code (first 240 chars)**:
```
import json
from data_helpers import save_result

# Read the JSON file directly to examine its structure
json_path = '/Users/bingxuan/Desktop/repo/data_analytics_agent/public/input/task_418/creatinine_age_analysis_result.json'

try:
    wit
```
**Helper opportunities** (by keyword match): `safe_read_json_df`
**Helpers actually used in final code**: `(none)`


---

## Category C — Code-Error Exhaustion — 1 tasks

### task_415 (hard, 8 iter)
**Q**: What is the constructor reference name of the champion in the 2009 Singapore Grand Prix? Please give its website.

**Last Judge reasoning** (truncated): RED FLAGS FOUND: 1) Empty answer returned - no constructorRef or url found. 2) Critical SQL error: 'Binder Error: Table

**Final code (first 240 chars)**:
```
import duckdb
import pandas as pd
from data_helpers import safe_read_json_df, save_result

# Connect to DuckDB
con = duckdb.connect()

# Load constructors from JSON - let DuckDB auto-detect schema
con.execute("""
    CREATE TABLE constructo
```
**Helper opportunities** (by keyword match): `(none detected)`
**Helpers actually used in final code**: `safe_read_json_df`


---

## Category D — Other — 0 tasks
(none)
